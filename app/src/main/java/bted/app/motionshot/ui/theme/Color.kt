package bted.app.motionshot.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Core Surfaces (camera / dark areas) ───
val MotionSurface = Color(0xFF0A0A0C)
val MotionSurfaceVariant = Color(0xFF141416)
val MotionSurfaceElevated = Color(0xFF1C1C20)

// ─── Accent ───
val MotionBlue = Color(0xFF448AFF)
val MotionBlueDim = Color(0xFF1A3A6E)

// ─── Text on dark surfaces ───
val MotionTextPrimary = Color(0xFFF0F0F2)
val MotionTextSecondary = Color(0xFFB0B0B4)
val MotionTextMuted = Color(0xFF6E6E72)

// ─── Light panel (bottom sheet controls) ───
val MotionPanel = Color(0xFFFAFAFC)
val MotionPanelText = Color(0xFF1A1A1E)
val MotionPanelTextMuted = Color(0xFFAAAAAE)
val MotionPanelRing = Color(0xFFE4E4E8)
val MotionPanelDivider = Color(0xFFF0F0F2)

// ─── Utility ───
val MotionScrim = Color(0xCC000000)
val MotionRing = Color(0xFF2A2A2E)